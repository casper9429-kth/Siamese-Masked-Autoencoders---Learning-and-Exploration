{
    "comment": "All hyperparameters are not used or relevant, but are put in as placeholders until development is complete",
    "batch_size": 64,
    "epochs": 400,
    "accum_iter": 1,
    "model": "mae_vit_large_patch16",
    "input_size": 224,
    "mask_ratio": 0.75,
    "norm_pix_loss": false,
    "weight_decay": 0.05,
    "lr": null,
    "blr": 0.001,
    "min_lr": 0.0,
    "warmup_epochs": 40,
    "data_path": "/datasets01/imagenet_full_size/061417/",
    "output_dir": "./output_dir",
    "log_dir": "./output_dir",
    "device": "cuda",
    "seed": 0,
    "resume": "",
    "start_epoch": 0,
    "num_workers": 10,
    "pin_mem": false,
    "world_size": 1,
    "local_rank": -1,
    "dist_on_itp": false,
    "dist_url": "env://"
}

